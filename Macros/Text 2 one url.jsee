#include "common.js"
// 导入公共函数定义

// ------------------------------------------------------------------------------------------------

// 🪴  0  使用场景说明

// ------------------------------------------------------------------------------------------------

// 数据输入：各种聊天的文本格式的数据
// 数据输出：自己需要的某一类网址
// 数据特征：某一类网址，网址内容会随着时间更新而更新不会过时，比如 github.com

// ------------------------------------------------------------------------------------------------

// 🪴  1  选定需要的网址类型

// var URL_Front = "https://github.com"
var URL_Front = "https://arxiv.org"
Extract_Lines("Normal", URL_Front);						// 因为没有时间限制，所以可以直接选取需要的网址信息
Delete_Indent();										// 删除缩进

// ------------------------------------------------------------------------------------------------

// 🪴  2  从文本行中提取网址并覆盖原来已有的文本

Extract_URLs_From_Lines(URL_Front);						// 掐头去尾

// ------------------------------------------------------------------------------------------------

// 🪴  3  删除夹杂中文的网址

Delete_Lines("Regex", "[^\\x00-\\xff]");            	// 直接删除中文的情况，这个比例很小很小，可以不管

// ------------------------------------------------------------------------------------------------

// 🪴  4  删除网址内部的定位信息 - Parameters & Anchor

Delete_Parameters_And_Anchors();						// 删除网址内部的参数和锚点
Clean_Text();											// 删除重复行，按字母排序

// ------------------------------------------------------------------------------------------------

// 最终会生成一个文件，文件内容为选定的某一类的筛选之后的网址列表。
